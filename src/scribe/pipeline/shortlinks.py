"""go.oklabs.uk shortlinks. TODO(task#7).

Port create_verified_shortlink from finalize_scribe_job.py.
Shortlinks point at scribe's own web-UI transcript/summary pages.
"""


def make_shortlink(target_url: str) -> str:
    raise NotImplementedError("task#7")
