#!/bin/bash
# Auto-generated by maestro pipeline — verification commands for issue #411.
# Every named check runs for real. `set -euo pipefail` means any failure aborts
# with a non-zero exit, so "All verifications passed" prints only when it did.
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT"

echo '=== Design guard scripts (Req 4, 8: tokens/recipes/nesting/forbidden primitives) ==='
bash scripts/check-design-tokens.sh
bash scripts/check-design-recipes.sh
bash scripts/check-design-nesting.sh
bash scripts/check-forbidden-primitives.sh

echo '=== SPA: lint + build + unit tests (Req 1, 2, 5: command-palette upload entry point) ==='
(
  cd web/spa
  bun install --frozen-lockfile
  bun run lint
  bun run build
  bun test tests
)

echo '=== Python: deps + lint + tests (Req 3, 6, 7: #408 upload reuse, ffmpeg media, mobile parity) ==='
uv sync --frozen --all-extras --all-groups
uv run ruff check src tests
uv run pytest -q

echo '=== Req 9: runtime upload on http://10.10.0.13:13120/ ==='
echo 'MANUAL (post-deploy): upload a real media file from a desktop-width browser'
echo 'and confirm a transcript is produced. Not gated here — requires a live deploy.'

echo 'All verifications passed.'
